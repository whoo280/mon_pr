name: hello
description: A Flutter application for reporting emergencies.

publish_to: 'none' # Remove this line if you want to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  location: ^4.3.0
  url_launcher: ^6.0.20
  share_plus: ^4.0.10
  connectivity_plus: ^2.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    # Ajoutez ici vos fichiers d'assets si nécessaire

